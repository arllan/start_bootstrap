// aprendendo ajax

/* o metodo ajax no Jquery e chamado da seguinte formas "$.ajax()" dentro do método e recebido um objeto 

$.ajax({
 url: 'meuscript.php', // destino da requisição
 method: 'post', // metodo da requisição POST ou GET... 
 data: { // o data e os parametros que vão ser enviados pela requisição
  nome: 'Daniel', // parametro a ser enviado. Fica na seguinte ordem nome e informação 
  idade: 19, // parametro a ser enviado. Fica na seguinte ordem nome e informação 
  endereco: 'Rua Lobato, Cobilandia' // parametro a ser enviado. Fica na seguinte ordem nome e informação 
 } // fim so data
}); // fim do objeto

*/